import {createContext, ReactNode} from "react";
import {CreateProposalForm} from "@interfaces/create-proposal.interface";


export const CreateProposalContext = createContext({

});
export const CreateProposalProvider = ({children}: { children: ReactNode }) => {

}
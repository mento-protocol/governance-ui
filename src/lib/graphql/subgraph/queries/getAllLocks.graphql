query getAllLocks {
  locks(where: { relocked: false }, orderBy: lockId, orderDirection: desc) {
    lockId
    owner {
      id
    }
    amount
    time
    slope
    cliff
    lockCreate {
      id
      timestamp
    }
    delegate {
      id
    }
  }
}

query getAllLocks {
  locks(where: { relocked: false }) {
    lockId
    owner {
      id
    }
    amount
    time
    slope
    cliff
    lockCreate {
      id
      timestamp
    }
    delegate {
      id
    }
  }
}

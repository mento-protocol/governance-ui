query getLocks($address: String) {
  locks(where: { owner: $address }, orderBy: lockId, orderDirection: desc) {
    lockId
    owner {
      id
    }
    amount
    time
    slope
    cliff
    lockCreate {
      id
      timestamp
    }
  }
}

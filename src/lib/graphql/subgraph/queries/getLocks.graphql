query getLocks($address: String) {
  locks(where: { owner: $address }) {
    lockId
    owner {
      id
    }
    amount
    time
    slope
    cliff
    lockCreate {
      id
      timestamp
    }
  }
}
